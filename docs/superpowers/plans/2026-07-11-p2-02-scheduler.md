# P2-02 Scheduler Implementation Plan

**Goal:** Add a Redis-backed Beijing-time scheduler without implementing P2-03 locks.

1. Write failing tests for source intervals, pause/resume, manual trigger, next run, and restart suppression.
2. Implement `scheduler.py` with a Redis state repository and queue dispatch callback.
3. Add a scheduler Compose service and local Redis integration test.
4. Run quality gates, a 10-minute cadence test, update README and check P2-02 only after all acceptance passes.
